# By default, CloudStore data API endpoint listens on all interfaces while
# the token store only listens on localhost for security reasons.

# postgres password for user cf_cloudstore
CLOUDSTORE_PASSWORD="dhY8AGhJ3Z"

CLOUDSTORE_DOMAIN="localhost"

CLOUDSTORE_PROTOCOL="http"
CLOUDSTORE_PORT=10002
CLOUDSTORE_ORIGIN="*"

TOKENSTORE_PORT=10004
TOKENSTORE_IP="{127, 0, 0, 1}"
TOKENSTORE_DOMAIN="localhost"

ROUTER_PROTOCOL="http"
ROUTER_PORT=10003
ROUTER_ORIGIN="*"
